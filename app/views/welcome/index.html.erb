	<!-- This will be our main container -->
	<div id="main">
	</div>

	<div id='canvas'>
		<h3>Yesterday's REPOS!</h3>
		
		<label>Select another day:</label>
		<%= collection_select(:forkday, :forkday_id, @forks, :date, :date) %>
		<%= submit_tag 'change date', { :id => 'button'} %>
		<div id='container'>
		</div>	
	</div>